# [작업지시서] 벨라리치 진단테스트 코드베이스 내용 분석

**수신:** Antigravity
**대상 경로:** `C:\Users\LG\orca\BellaRich`
**작성일:** 2026-08-30
**목적:** 미니맥스가 만들어 놓은 벨라리치 진단테스트지의 **내용(콘텐츠·로직·데이터)** 을 분석해 인수인계 문서를 만든다. 코드 수정은 하지 않는다. **읽기 전용(read-only) 분석.**

---

## 0. 분석 대상 파일

| 파일 | 크기 | 성격 |
|---|---|---|
| `index.html` | 2,646줄 / 96KB | 단일 페이지 앱 전체 (마크업 + CSS + JS 전부 인라인) |
| `quiz-data.js` | 716줄 / 35KB | 진단 문항·채점·결과 카피 데이터 |
| `venn-diagram.png` | 46KB | 히어로 영역 벤다이어그램 이미지 |
| `README.md` | 2줄 | "벨라리치 기업 상담 전 테스트지" 한 줄 설명뿐 |
| `.git` | 16 커밋 | v3 → v6.x → v7.2.6 버전 이력 |

전부 다 읽어라. 요약본으로 대충 넘기지 말 것.

---

## 1. 반드시 답해야 할 질문

### A. 서비스 정체 파악
1. 이 페이지의 **비즈니스 목적**은 무엇인가? (`<title>`: "법인 자산 1분 건강검진 | 벨라리치")
2. **타깃 사용자**는 누구이며, 어떤 심리적 흐름으로 리드(상담 신청)까지 유도하는가?
3. 화면 플로우를 정확히 도식화하라. 현재 확인된 스크린 ID:
   `#screen-intro` → `#screen-category` → `#screen-question` → `#screen-consent` → `#screen-result`
   - 각 화면의 전환 조건, 되돌아가기(`goBack()`) 가능 지점, 이탈 지점
   - `#q0-section`(사전질문 q0)이 이후 분기에 어떤 영향을 주는지

### B. 진단 데이터 구조 (`quiz-data.js`)
4. `CATEGORIES` 객체의 5개 카테고리를 각각 정리하라:
   `tax`(세액감면) / `realestate`(부동산 활용) / `succession`(가업승계) / `retirement`(은퇴설계) / `management`(경영지원)
   - 카테고리별 **문항 수, 문항 전문, 선택지, 각 선택지 배점**
   - 표로 정리해 한눈에 비교 가능하게
5. `CATEGORY_META` 배열은 `CATEGORIES`와 어떻게 연결되는가? 중복/불일치가 있는가?
6. **채점 로직 전체 역설계**: 원점수 → 100점 환산 → 등급(S/A/B/C 추정) 매핑 규칙을 수식으로 명시하라.
   - 등급 경계값(컷오프)이 몇 점인지 정확히
   - 카테고리별로 컷오프가 다른지, 동일한지
7. 등급별 결과 카피(`title` / 요약 / CTA 문구 / `findings`)를 등급×카테고리 매트릭스로 전부 추출하라.
   (예: 각 카테고리에 "지금 구조, 다음 10년 절세 로드맵으로" ~ "지금 바로 1:1 진단을 받아보세요" 4단계 CTA가 존재)

### C. 백엔드 연동
8. `index.html:1935`의 `APPS_SCRIPT_URL` (Google Apps Script 웹앱)로 **정확히 어떤 데이터가 전송**되는가?
   - 전송 payload의 필드를 하나도 빠짐없이 나열
   - `fetch` / `navigator.sendBeacon` 2가지 경로(1935~2035줄 부근)를 쓰는 이유와 각각의 발동 조건
   - 실패 시 처리(사용자에게 보이는 결과 / 무음 실패 여부)
9. **개인정보 수집 항목**을 정리하라. (`#consent-email`, `#consent-collection` 필수 동의, `#consent-newsletter` 선택 동의)
   - 수집 항목 대비 동의 문구가 충분한지, 개인정보보호법 관점의 리스크 지적
10. 결과 PDF/메일 발송은 클라이언트가 하는가, Apps Script가 하는가? 코드 근거를 들어 판단하라.

### D. 버전 이력 해석 (`git log`)
11. 커밋 로그를 읽고 **제품이 어떤 방향으로 진화했는지** 서술하라. 특히:
    - `v6.5`: Venn 교집합 색 overlay **원복** → 왜 되돌렸을 것으로 보이는가
    - `v7`: 5개 카테고리 확장 + 이메일 수신 동의 + "결과는 메일로" 전환 → **결과를 화면에서 메일로 옮긴 설계 변경의 의미**
    - `v7.1 → v7.2.4 → v7.2.5(롤백) → v7.2.6`: Apps Script URL이 4번 바뀌고 한 번 롤백됨.
      커밋 메시지에 "메일 안 오던 문제"가 명시됨 → **이 장애의 원인 가설과 재발 방지책**을 제시하라.

### E. 코드 품질 · 리스크
12. 2,646줄 단일 HTML에 마크업/CSS/JS가 전부 인라인이다. 유지보수 관점 리스크와 분리 제안.
13. 하드코딩된 값(Apps Script URL, 상담 신청 링크 `#cta-btn`, 점수 컷오프, 카피)을 모두 찾아 **설정 파일로 빼야 할 목록**을 만들어라.
14. 접근성: `aria-label`, `aria-live`, `#main-content` 등 이미 적용된 부분과 **누락된 부분**을 구분해 지적.
15. 명백한 버그·엣지케이스 후보를 목록화하라. (예: 뒤로가기 시 점수 중복 가산, 이메일 검증 우회, 중복 제출, 새로고침 시 상태 소실)

---

## 2. 산출물

`C:\Users\LG\orca\BellaRich\docs\` 아래에 마크다운으로 생성:

1. `ANALYSIS-01-개요와플로우.md` — A 섹션 답변 + 화면 플로우 다이어그램(mermaid)
2. `ANALYSIS-02-문항과채점.md` — B 섹션 답변 + 문항 전문 표 + 채점 수식 + 등급 매트릭스
3. `ANALYSIS-03-백엔드와개인정보.md` — C 섹션 답변 + payload 스펙 표
4. `ANALYSIS-04-버전이력.md` — D 섹션 답변 + 타임라인
5. `ANALYSIS-05-리스크와개선제안.md` — E 섹션 답변 + 우선순위(P0/P1/P2) 부여
6. `HANDOFF.md` — 위 5개를 A4 2장 분량으로 압축한 **경영진용 인수인계 요약**

---

## 3. 준수사항

- **코드를 수정하지 마라.** `index.html`, `quiz-data.js`, `venn-diagram.png`, `.git`은 절대 건드리지 않는다. 산출물은 `docs/` 신규 생성만.
- 모든 주장에 **파일명:줄번호** 근거를 달아라. 근거 없는 추측은 "추정:" 이라고 명시.
- 코드에 없어서 알 수 없는 것은 지어내지 말고 **"확인 불가 — 확인 필요 항목"** 목록에 넣어라.
- 산출물은 한국어로 작성.

---

## 4. 참고 — 다른 위치의 관련 자료 (필요시에만 열람, 역시 읽기 전용)

이 저장소는 배포용 스냅샷이라 문서가 거의 없다. 맥락이 더 필요하면:

- `C:\Users\LG\Desktop\미니맥스\bellarich-test` — 미니맥스 **메인 작업 폴더** (최신). `AGENTS.md`, `HANDOFF.md`, `apps-script.gs`(서버 코드 전문), 이메일 템플릿 v9, PDF 샘플, 회의록 `meeting-1.txt`/`meeting-2.txt`, `BellaRich_카테고리별질문지_v7.xlsx`
- `C:\Users\LG\.minimax\workspace\bellarich-v8-backup` — 위와 거의 동일 + `docs/` 폴더
- `C:\Users\LG\.minimax\workspace\bellarich-v5|v6|v6.2|v6.5|v7-backup` — 버전별 스냅샷 (버전 간 diff 분석용)
- `C:\Users\LG\Desktop\소상공인\` — 질문지 엑셀, 방문인터뷰 스크립트 docx

특히 **8번(Apps Script payload)과 10번(메일 발송 주체)** 은 `bellarich-test\apps-script.gs`(35KB, 서버 측 코드)를 함께 봐야 정확히 답할 수 있다. 이 저장소만으로 답이 안 나오면 그 파일을 참조하고, 참조했음을 명시하라.
